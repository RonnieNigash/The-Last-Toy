#include "radiance.h"

Vector *intensity(Ray *ray, uint8_t ray_depth, uint8_t randomSeed)
{
	// returns vector which estimates the intensity of a ray	
	
	return initVector(0, 0, 0); // placeholder for implementation

	//while ( 2 ) {
		// if ray intersects, return 0 intensity
		// else hits object
		// Implement ray tracing algo 
	//}
}
